# 专题深度线（工程 A）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给一手内参加第三条内容线「专题深度」：选题初筛 → 骨架生成器产出带官方素材的 draft.md → 喂给已有 article-harness 打磨成稿 → 公众号发布。首篇 Claude Tag。

**Architecture:** 只新增「选题初筛 + 骨架生成器」两个纯函数（`scripts/firsthand/deepdive.py`）+ 一个 wiring skill（`skills/firsthand-deepdive`）。Writer-Reviewer 循环与审核学习全部复用 `Engineer/skills/article-harness`（不重造）。选题初筛复用 Feature B 的 `index.json` + Feature A 的 `cluster_same_event`。

**Tech Stack:** Python 3（复用 `query.py`/`pipeline.py`/`adapters.py`）、article-harness（bash）、markdown skill。

**落点决策（来自 spec）：**
- skill + draft 都进 ai-frontier-daily（做 AI 内容不跳仓）。
- draft 输出 `content/deepdive/<date>_<slug>/draft.md`（新目录）。
- article-harness 就地复用 `Engineer/skills/article-harness`；工程 B 收敛后仅改一处调用路径。

---

## File Structure

| 文件 | 职责 |
|------|------|
| `scripts/firsthand/deepdive.py` | 选题初筛 `candidates` + 骨架生成 `build_skeleton` + 落盘 `write_draft` |
| `tests/firsthand/test_deepdive.py` | 上述纯函数测试 |
| `skills/firsthand-deepdive/SKILL.md` | wiring：列候选 → 用户选 → 生成 draft → 提示跑 article-harness |
| `content/deepdive/<date>_<slug>/draft.md` | 产出草稿（运行时生成） |
| `.gitignore` | 忽略 article-harness 中间产物 `.harness-workspace/` |
| `CLAUDE.md` | 补「第三条内容线：专题深度」说明 |

复用（不改）：`scripts/firsthand/query.py`（`load_index`/`recent`）、`pipeline.py`（`cluster_same_event`）、`adapters.py`（`fetch_article_text`）。

测试：`python3 -m pytest tests/firsthand/ -v`。

---

## Task 1: 选题初筛 candidates

**Files:**
- Create: `scripts/firsthand/deepdive.py`
- Test: `tests/firsthand/test_deepdive.py`

把内参近 N 天条目按「同事件」聚成候选（跨源同事件算一个候选），供用户选。复用 `recent` + `cluster_same_event`。

- [ ] **Step 1: 写 failing test**

`tests/firsthand/test_deepdive.py`:
```python
from scripts.firsthand.deepdive import candidates


def _it(title, source, resource, detected, summary="s"):
    return {"title": title, "source": source, "resource": resource,
            "detected": detected, "summary": summary, "published": detected[:10]}


def test_candidates_clusters_same_event_recent():
    items = [
        _it("Introducing Claude Tag", "anthropic-news",
            "https://www.anthropic.com/news/introducing-claude-tag", "2026-06-24T07:49:00+08:00"),
        _it("Agent identity in Claude Tag", "claude-blog",
            "https://claude.com/blog/agent-identity-access-model", "2026-06-24T07:49:00+08:00"),
        _it("Old thing", "openai-news", "https://openai.com/news/old", "2026-01-01T00:00:00+08:00"),
    ]
    cands = candidates(items, days=7, today="2026-06-24")
    # 近 7 天：Claude Tag 两源归一个候选；Old thing 超窗被排除
    assert len(cands) == 1
    assert {a["resource"] for a in cands[0]} == {
        "https://www.anthropic.com/news/introducing-claude-tag",
        "https://claude.com/blog/agent-identity-access-model",
    }


def test_candidates_empty_when_all_old():
    items = [_it("x", "claude-blog", "u", "2026-01-01T00:00:00+08:00")]
    assert candidates(items, days=7, today="2026-06-24") == []
```

- [ ] **Step 2: 运行确认失败**

Run: `python3 -m pytest tests/firsthand/test_deepdive.py -v`
Expected: FAIL（ModuleNotFoundError: scripts.firsthand.deepdive）

- [ ] **Step 3: 实现**

`scripts/firsthand/deepdive.py`:
```python
"""专题深度线：选题初筛 + 骨架生成器。
复用内参 index（query.recent）+ 同事件聚合（pipeline.cluster_same_event），
产出带官方素材的 draft.md，喂给 article-harness 打磨。
"""
from pathlib import Path

from .query import recent
from .pipeline import cluster_same_event
from .adapters import fetch_article_text
from .store import slugify


def candidates(items: list[dict], days: int, today: str) -> list[list[dict]]:
    """内参近 days 天条目 → 同事件聚成候选（每候选 = 一个事件的一组文章），按时间倒序。
    cluster_same_event 用 title 字段，index 条目已带 title/source。"""
    recents = recent(items, days, today)
    groups = cluster_same_event(recents)
    # 组内按 detected 倒序；组间按组内最新 detected 倒序
    for g in groups:
        g.sort(key=lambda a: a.get("detected") or "", reverse=True)
    groups.sort(key=lambda g: g[0].get("detected") or "", reverse=True)
    return groups
```

- [ ] **Step 4: 运行确认通过**

Run: `python3 -m pytest tests/firsthand/test_deepdive.py -v`
Expected: PASS（2 passed）

- [ ] **Step 5: Commit**

```bash
git add scripts/firsthand/deepdive.py tests/firsthand/test_deepdive.py
git commit -m "feat(deepdive): 选题初筛 candidates（同事件聚合候选）"
```

---

## Task 2: 骨架生成器 build_skeleton

**Files:**
- Modify: `scripts/firsthand/deepdive.py`
- Test: `tests/firsthand/test_deepdive.py`

把一个候选（一组同事件文章）渲染成 article-harness 能吃的 `draft.md`：frontmatter `sources`（reference 类型给官方 URL）+ 五段骨架（带 TODO）+ 官方原文素材区。

- [ ] **Step 1: 追加 failing test**

在 `tests/firsthand/test_deepdive.py` 末尾追加：
```python
from scripts.firsthand.deepdive import build_skeleton


def test_build_skeleton_structure():
    cluster = [
        _it("Introducing Claude Tag", "anthropic-news",
            "https://www.anthropic.com/news/introducing-claude-tag", "2026-06-24T07:49:00+08:00",
            summary="Claude 作为团队成员加入 Slack。"),
        _it("Agent identity in Claude Tag", "claude-blog",
            "https://claude.com/blog/agent-identity-access-model", "2026-06-24T07:49:00+08:00",
            summary="agent 独立身份访问模型。"),
    ]
    md = build_skeleton(cluster, fetch_text_fn=lambda u: f"正文@{u}", feature_available=False)
    # frontmatter sources 含两个官方 URL（reference）
    assert "type: reference" in md
    assert "https://www.anthropic.com/news/introducing-claude-tag" in md
    assert "https://claude.com/blog/agent-identity-access-model" in md
    # 五段骨架都在
    for sec in ["① 这是什么", "② 功能详解", "③ 官方案例", "④ 实操截图", "⑤ 作者点评"]:
        assert sec in md
    # 不可体验 → ④ 标注暂不可体验
    assert "暂不可体验" in md
    # 官方原文素材被抓进来
    assert "正文@https://www.anthropic.com/news/introducing-claude-tag" in md
    # 内参摘要作为起点
    assert "Claude 作为团队成员加入 Slack。" in md


def test_build_skeleton_feature_available_marks_todo_screenshot():
    cluster = [_it("X feature", "claude-blog", "https://claude.com/blog/x",
                   "2026-06-24T00:00:00+08:00")]
    md = build_skeleton(cluster, fetch_text_fn=lambda u: "t", feature_available=True)
    assert "待补截图" in md
    assert "暂不可体验" not in md
```

- [ ] **Step 2: 运行确认失败**

Run: `python3 -m pytest tests/firsthand/test_deepdive.py -v`
Expected: FAIL（ImportError: cannot import name 'build_skeleton'）

- [ ] **Step 3: 实现**（追加到 `deepdive.py`）

```python
def build_skeleton(cluster: list[dict], fetch_text_fn=fetch_article_text,
                   feature_available: bool = False) -> str:
    """一组同事件文章 → article-harness 草稿 markdown。
    frontmatter sources 给官方 URL（type: reference），正文为五段骨架 + TODO，
    末尾附官方原文素材（供 Writer 整理/翻译，不直接入正文）。"""
    primary = cluster[0]
    title = primary.get("title") or primary.get("resource")

    # frontmatter sources（官方一手 → reference）
    src_lines = ["sources:"]
    for a in cluster:
        src_lines.append(f"  - type: reference")
        src_lines.append(f"    url: {a['resource']}")
        src_lines.append(f"    note: {a['source']} 官方原文，引用时按此注明出处")

    seen_summaries = "；".join(a.get("summary") or "" for a in cluster if a.get("summary"))

    screenshot_todo = ("TODO: 待补截图/录屏（功能可体验时补操作截图）"
                       if feature_available else
                       "TODO: 该功能**暂不可体验**——本节标注「暂不可体验，待开放后补截图/录屏」")

    body = [
        "---",
        f"title: {title}",
        *src_lines,
        "---",
        "",
        f"# {title}",
        "",
        f"> 内参摘要（起点，非成稿）：{seen_summaries}",
        "",
        "## ① 这是什么（向普通用户）",
        "TODO: 用一句话向普通用户说清这是什么、能干嘛。素材见文末官方原文。",
        "",
        "## ② 功能详解",
        "TODO: 解决什么痛点 / 怎么用 / 关键限制。基于官方原文整理，不堆术语。",
        "",
        "## ③ 官方案例整理 + 翻译",
        "TODO: 整理官网/博客的 use case 并译为中文，保留官方限定词（may/reportedly→可能/据称），不夸大。",
        "",
        "## ④ 实操截图/录屏",
        screenshot_todo,
        "",
        "## ⑤ 作者点评（变量生活视角）",
        "TODO: 尹家露视角的判断（可选）。",
        "",
        "---",
        "## 官方原文素材（供 Writer 整理/翻译，不直接入正文）",
    ]
    for a in cluster:
        text = ""
        try:
            text = fetch_text_fn(a["resource"])
        except Exception as e:
            text = f"(抓取失败: {e})"
        body.append(f"### [{a['source']}] {a.get('title') or ''}")
        body.append(f"原文: {a['resource']}")
        body.append(text[:6000])
        body.append("")
    return "\n".join(body)
```

- [ ] **Step 4: 运行确认通过**

Run: `python3 -m pytest tests/firsthand/test_deepdive.py -v`
Expected: PASS（4 passed）

- [ ] **Step 5: Commit**

```bash
git add scripts/firsthand/deepdive.py tests/firsthand/test_deepdive.py
git commit -m "feat(deepdive): 骨架生成器 build_skeleton（五段 + 官方素材）"
```

---

## Task 3: 落盘 write_draft

**Files:**
- Modify: `scripts/firsthand/deepdive.py`
- Test: `tests/firsthand/test_deepdive.py`

把骨架写到 `content/deepdive/<date>_<slug>/draft.md`，返回路径。

- [ ] **Step 1: 追加 failing test**

```python
from scripts.firsthand.deepdive import write_draft


def test_write_draft_path_and_content(tmp_path):
    cluster = [_it("Introducing Claude Tag", "anthropic-news",
                   "https://www.anthropic.com/news/introducing-claude-tag",
                   "2026-06-24T07:49:00+08:00")]
    path = write_draft(tmp_path, cluster, today="2026-06-24",
                       fetch_text_fn=lambda u: "正文", feature_available=False)
    assert path.parent.name == "2026-06-24_introducing-claude-tag"
    assert path.name == "draft.md"
    assert path.exists()
    assert "## ① 这是什么" in path.read_text(encoding="utf-8")
```

- [ ] **Step 2: 运行确认失败**

Run: `python3 -m pytest tests/firsthand/test_deepdive.py::test_write_draft_path_and_content -v`
Expected: FAIL（ImportError: cannot import name 'write_draft'）

- [ ] **Step 3: 实现**（追加到 `deepdive.py`）

```python
def write_draft(content_root, cluster: list[dict], today: str,
                fetch_text_fn=fetch_article_text, feature_available: bool = False) -> Path:
    """写 <content_root>/deepdive/<date>_<slug>/draft.md，返回路径。"""
    slug = slugify(cluster[0]["resource"])
    d = Path(content_root) / "deepdive" / f"{today}_{slug}"
    d.mkdir(parents=True, exist_ok=True)
    path = d / "draft.md"
    path.write_text(build_skeleton(cluster, fetch_text_fn, feature_available), encoding="utf-8")
    return path
```

- [ ] **Step 4: 运行确认通过**

Run: `python3 -m pytest tests/firsthand/ -v`
Expected: 全 PASS（含新增 5 个 deepdive 用例）

- [ ] **Step 5: Commit**

```bash
git add scripts/firsthand/deepdive.py tests/firsthand/test_deepdive.py
git commit -m "feat(deepdive): write_draft 落盘 content/deepdive/"
```

---

## Task 4: firsthand-deepdive skill（wiring）

**Files:**
- Create: `skills/firsthand-deepdive/SKILL.md`
- Modify: `.gitignore`

- [ ] **Step 1: 写 SKILL.md**

`skills/firsthand-deepdive/SKILL.md`:
```markdown
---
name: firsthand-deepdive
description: 专题深度线——对一个值得深挖的功能点（如 Claude Tag）生成深度文章草稿。从内参索引列「值得深挖」候选→用户选→抓官方素材生成五段骨架 draft→交 article-harness 打磨。用户说「深挖 X / 做个 X 的专题 / 专题深度」时使用。
---

# 专题深度线

内参的第三条内容线：把官方一手的重要功能点（模型发布/重大功能）做成面向普通用户的深度文章。

## 流程

### 1. 选题初筛（AI 列候选，用户选）
```bash
python3 -c "import json,datetime; from scripts.firsthand.query import load_index; from scripts.firsthand.deepdive import candidates; \
today=datetime.datetime.now(datetime.timezone(datetime.timedelta(hours=8))).date().isoformat(); \
[print(i+1, '|', g[0]['title'], '|', ', '.join(a['source'] for a in g)) for i,g in enumerate(candidates(load_index('data/firsthand'), 7, today))]"
```
把候选清单（每个 = 一个事件，可能多源）报给用户，**用户选一个**。
也可用户直接指定（不在索引里时，手工构造 cluster：每篇 `{title, source, resource, summary, detected}`）。

### 2. 生成骨架 draft
判断该功能**当前能否体验**（如 Claude Tag 暂不可体验 → `feature_available=False`）：
```bash
python3 -c "from scripts.firsthand.query import load_index; from scripts.firsthand.deepdive import candidates, write_draft; \
import datetime; today=datetime.datetime.now(datetime.timezone(datetime.timedelta(hours=8))).date().isoformat(); \
cands=candidates(load_index('data/firsthand'), 7, today); \
p=write_draft('content', cands[<选中序号-1>], today, feature_available=False); print('draft:', p)"
```
产出 `content/deepdive/<date>_<slug>/draft.md`：五段骨架 + frontmatter sources（官方 URL）+ 官方原文素材。

### 3. 交 article-harness 打磨（复用，不重造）
```bash
bash /Users/jialu/Documents/Engineer/skills/article-harness/harness.sh content/deepdive/<date>_<slug>/draft.md
```
Writer 整理润色 ↔ Reviewer 按 reviewer.md 评分迭代，输出成稿。**用户只审核**。

### 4. 审核反馈沉淀（已有机制）
用户对成稿的每条可泛化反馈 → 沉淀进 `article-harness/reviewer.md`（含专题深度专属维度：官方案例翻译准确 / 对普通用户友好不堆术语 / 截图占位标注清楚）。说一次，后续自动检查。

### 5. 发布
终审后 → `wechat-official-draft` skill 排版发布。

## 与其他线的关系
- 选题来源：内参 `data/firsthand/index.json`（官方一手）。
- 不重造 Writer-Reviewer（复用 article-harness）；工程 B 收敛 skills 后仅改第 3 步路径。
```

- [ ] **Step 2: gitignore article-harness 中间产物**

在 `.gitignore` 追加：
```
# article-harness 迭代中间产物（成稿在 draft 同目录，工作区不提交）
content/deepdive/**/.harness-workspace/
```

- [ ] **Step 3: Commit**

```bash
git add skills/firsthand-deepdive/SKILL.md .gitignore
git commit -m "feat(deepdive): firsthand-deepdive skill + 忽略 harness 工作区"
```

---

## Task 5: Claude Tag 首篇（首跑 + 种审核标准）

**Files:**
- Create: `content/deepdive/2026-06-24_introducing-claude-tag/draft.md`（生成）
- Modify: `Engineer/skills/article-harness/reviewer.md`（补专题深度维度）

- [ ] **Step 1: 生成 Claude Tag 骨架**（Claude Tag 在 PR #49 未合并，手工构造 cluster）

Run:
```bash
python3 -c "
from scripts.firsthand.deepdive import write_draft
cluster=[
 {'title':'Introducing Claude Tag','source':'anthropic-news','resource':'https://www.anthropic.com/news/introducing-claude-tag','summary':'Claude 作为团队成员加入 Slack，被 @Claude 即可委派任务。','detected':'2026-06-24T07:49:00+08:00'},
 {'title':'Agent identity in Claude Tag: a new access model','source':'claude-blog','resource':'https://claude.com/blog/agent-identity-access-model','summary':'Claude 以独立 agent 身份行事，权限从 per-user 转 per-channel。','detected':'2026-06-24T07:49:00+08:00'},
]
p=write_draft('content', cluster, '2026-06-24', feature_available=False)
print('生成:', p)
"
```
Expected: 打印 `生成: content/deepdive/2026-06-24_introducing-claude-tag/draft.md`，文件含五段骨架 + 抓到的官方原文。

- [ ] **Step 2: 跑 article-harness 打磨**

Run:
```bash
bash /Users/jialu/Documents/Engineer/skills/article-harness/harness.sh content/deepdive/2026-06-24_introducing-claude-tag/draft.md 5
```
Expected: 输出成稿路径（`.harness-workspace/finished.md`），Reviewer 各维度 ≥7 PASS。若某维度反复 FAIL，把成稿现状交用户审核。

- [ ] **Step 3: 把成稿交用户审核，收集反馈**

把 `finished.md` 呈现给用户。用户给审核反馈（可泛化的写作/翻译/可读性规则）。

- [ ] **Step 4: 沉淀专题深度审核维度到 reviewer.md**

把用户反馈中可泛化的规则，写进 `/Users/jialu/Documents/Engineer/skills/article-harness/reviewer.md`（新增「专题深度」维度或黑名单条目）：
- 官方案例翻译准确（不误译/不夸大/保留限定词）
- 对普通用户友好（先讲"对用户意味着什么"，不堆术语）
- 截图/录屏占位标注清楚（未补不留空段冒充完成）

（具体条目按用户实际反馈写成可机械核对的规则，不写"尽量"模糊措辞。）

- [ ] **Step 5: Commit draft（成稿留本地工作区，draft 入仓）**

```bash
git add content/deepdive/2026-06-24_introducing-claude-tag/draft.md
git commit -m "content(deepdive): Claude Tag 专题首篇草稿"
```

> 公开仓注意：`content/deepdive/` 会进公开仓。Claude Tag 是已公开新闻、评论性内容，风险低。
> 若想草稿私有，可改为 gitignore `content/deepdive/` 仅本地——届时与用户确认。

---

## Task 6: 文档串联

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: CLAUDE.md 补第三条线**

在 `CLAUDE.md` 顶部内容线说明处追加：
```markdown
## 第三条内容线：专题深度（firsthand-deepdive）

- 对官方一手的重要功能点做面向普通用户的深度文章。流程：选题初筛（扫 `data/firsthand/index.json` 同事件聚合候选）
  → 用户选 → `scripts/firsthand/deepdive.py` 生成五段骨架 draft 到 `content/deepdive/<date>_<slug>/`
  → 交 `Engineer/skills/article-harness`（复用，不重造）Writer-Reviewer 打磨 → 用户审核 → 反馈沉淀进
  `article-harness/reviewer.md` → `wechat-official-draft` 发布。
- 触发：`firsthand-deepdive` skill（「深挖 X / 专题深度」）。
- article-harness 属通用内容生产 skills，后续由「skills 收敛」工程（独立仓 + skills.sh + 全局安装）统一管理，
  届时本线第 3 步调用路径改为全局安装路径。
```

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs(deepdive): CLAUDE.md 补第三条内容线说明"
```

---

## 完成标准

- [ ] `python3 -m pytest tests/firsthand/` 全绿（含新增 deepdive 用例）
- [ ] `firsthand-deepdive` skill 可被触发，能列候选 → 生成 draft
- [ ] Claude Tag 首篇 draft 生成、跑过 article-harness、成稿交用户审核
- [ ] 用户首批反馈沉淀进 `article-harness/reviewer.md`（专题深度维度）
- [ ] CLAUDE.md 记录第三条线
