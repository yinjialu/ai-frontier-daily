# 专题深度 → 小红书发布 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把专题深度成稿（`content/deepdive/<slug>/draft.md`）稳定转换为小红书图文卡（1080×1440 PNG）+ 笔记文案，复用现成的 render.js 渲染引擎与 publish.html / xiaohongshu-mcp 发布链路。

**Architecture:** 新增「卡片脚本 `xhs.json`」中间产物（AI 生成）→ 校验硬约束 → 转成 render.js 要的 DATA（`kind:"deepdive"`）→ 复用 `render.js`（不改）逐卡截图。卡片版式作为 `cards.js`/`cards.css` 的纯追加分支（不动早报现有版式）。发布复用现成 publish.html（加深度入口）。

**Tech Stack:** Python（校验/编排，pytest）、Node + Playwright（render.js 现成引擎）、HTML/CSS（卡片版式）、macOS `sips`（图片尺寸核验）。

---

## File Structure

| 文件 | 职责 |
|------|------|
| `scripts/deepdive/xhs_validate.py` | 校验 xhs.json 是否满足平台/版式硬约束（纯函数 + CLI）|
| `scripts/deepdive/render_xhs.py` | 编排：读 xhs.json → 校验 → 转 render DATA（cover 转绝对路径、kind=deepdive）→ 调 `node render.js` |
| `skills/ai-daily-digest/cards.js` | 加 `deepdiveCards(DATA)` 构建器 + `buildDeck` 按 `kind` 分发（纯追加）|
| `skills/ai-daily-digest/cards.css` | 加 `.card.deepdive` 版式（1080×1440 知识卡，纯追加）|
| `skills/ai-daily-digest/test_deepdive_cards.js` | deepdiveCards 的 node 断言测试 |
| `skills/deepdive-xiaohongshu/SKILL.md` | skill：AI 卡片脚本生成指令 + 运行步骤 |
| `publish.html` | 加「深度内容」入口，列 `output/deepdive/<slug>/` 图 + 文案 |
| `tests/deepdive/test_xhs_validate.py` | xhs_validate 单测 |
| `tests/deepdive/test_render_xhs.py` | render DATA 转换单测 |
| `tests/deepdive/fixtures/xhs_sample.json` | 测试用样例卡片脚本 |

**数据形状（贯穿全程，类型一致性基准）：**

`xhs.json`（AI 产出）：
```json
{
  "slug": "introducing-claude-tag",
  "cover": "assets/cover-xiaohei-agent-identity.png",
  "title": "Claude Tag：给 AI 发工牌",
  "cards": [
    {"heading": "AI 不再代表你", "lines": ["以前它借你的权限行事", "现在它有自己的账号"]}
  ],
  "caption": "正文文案…",
  "tags": ["AI", "Claude", "Agent"]
}
```

`render DATA`（`render_xhs.py` 转出、喂给 render.js）：在 xhs.json 基础上 **加 `"kind":"deepdive"`**、**`cover` 换成绝对路径或 null**，其余字段透传。

`card 对象`（`deepdiveCards` 产出、render.js 消费）：`{name, cls, w, h, inner}`，`w:1080, h:1440`，`cls` 形如 `"deepdive cover"` / `"deepdive"`，`inner` 为 `<div class="inner">…</div>` HTML。

---

## Task 1: xhs.json 硬约束校验

**Files:**
- Create: `scripts/deepdive/xhs_validate.py`
- Create: `scripts/deepdive/__init__.py`（若不存在）
- Test: `tests/deepdive/test_xhs_validate.py`
- Test fixture: `tests/deepdive/fixtures/xhs_sample.json`

硬约束（字数按 `len()`，中文 1 字符=1）：title ≤20、caption ≤1000、cards 4~8 张、每卡 heading ≤14、lines 为 list 且 ≤4 行、每行 ≤22、tags 1~10。

- [ ] **Step 1: 建合法样例 fixture**

`tests/deepdive/fixtures/xhs_sample.json`:
```json
{
  "slug": "introducing-claude-tag",
  "cover": "assets/cover.png",
  "title": "Claude Tag：给 AI 发工牌",
  "cards": [
    {"heading": "AI 不再代表你", "lines": ["以前它借你的权限行事", "现在它有自己的账号"]},
    {"heading": "权限按频道发", "lines": ["从 per-user", "到 per-channel"]},
    {"heading": "侧门风险", "lines": ["共享密钥打结", "一处泄漏全局沦陷"]},
    {"heading": "为什么现在做", "lines": ["Agent 要独立身份", "审计与撤销才可行"]}
  ],
  "caption": "一句话讲清 Claude Tag 是什么。",
  "tags": ["AI", "Claude", "Agent"]
}
```

- [ ] **Step 2: 写 failing test**

`tests/deepdive/test_xhs_validate.py`:
```python
import json
from pathlib import Path
from scripts.deepdive.xhs_validate import validate_xhs

FIX = Path(__file__).parent / "fixtures" / "xhs_sample.json"

def load():
    return json.loads(FIX.read_text(encoding="utf-8"))

def test_valid_sample_has_no_errors():
    assert validate_xhs(load()) == []

def test_title_too_long():
    d = load(); d["title"] = "标" * 21
    assert any("标题" in e for e in validate_xhs(d))

def test_caption_too_long():
    d = load(); d["caption"] = "字" * 1001
    assert any("文案" in e for e in validate_xhs(d))

def test_too_few_cards():
    d = load(); d["cards"] = d["cards"][:3]
    assert any("卡片数" in e for e in validate_xhs(d))

def test_too_many_cards():
    d = load(); d["cards"] = [d["cards"][0]] * 9
    assert any("卡片数" in e for e in validate_xhs(d))

def test_heading_too_long():
    d = load(); d["cards"][0]["heading"] = "标" * 15
    assert any("heading" in e for e in validate_xhs(d))

def test_too_many_lines():
    d = load(); d["cards"][0]["lines"] = ["x"] * 5
    assert any("行数" in e for e in validate_xhs(d))

def test_line_too_long():
    d = load(); d["cards"][0]["lines"] = ["字" * 23]
    assert any("每行" in e for e in validate_xhs(d))
```

- [ ] **Step 3: 运行确认失败**

Run: `cd /Users/jialu/ai-frontier-daily && python -m pytest tests/deepdive/test_xhs_validate.py -q`
Expected: FAIL（`ModuleNotFoundError: scripts.deepdive.xhs_validate`）

- [ ] **Step 4: 实现**

`scripts/deepdive/__init__.py`: 空文件（若 `scripts/__init__.py` 不存在也建一个空的）。

`scripts/deepdive/xhs_validate.py`:
```python
#!/usr/bin/env python3
"""校验小红书卡片脚本 xhs.json 是否满足平台与版式硬约束。

用法：python -m scripts.deepdive.xhs_validate <xhs.json>
不过线打印错误并退出码 1。
"""
import json
import sys
from pathlib import Path

TITLE_MAX = 20
CAPTION_MAX = 1000
CARDS_MIN, CARDS_MAX = 4, 8
HEADING_MAX = 14
LINES_MAX = 4
LINE_MAX = 22
TAGS_MIN, TAGS_MAX = 1, 10


def validate_xhs(data: dict) -> list[str]:
    errors: list[str] = []
    title = data.get("title", "")
    if not isinstance(title, str) or len(title) > TITLE_MAX:
        errors.append(f"标题需 ≤{TITLE_MAX} 字（当前 {len(title)}）")
    caption = data.get("caption", "")
    if not isinstance(caption, str) or len(caption) > CAPTION_MAX:
        errors.append(f"文案需 ≤{CAPTION_MAX} 字（当前 {len(caption)}）")
    cards = data.get("cards")
    if not isinstance(cards, list) or not (CARDS_MIN <= len(cards) <= CARDS_MAX):
        errors.append(f"卡片数需 {CARDS_MIN}~{CARDS_MAX} 张（当前 {len(cards) if isinstance(cards, list) else 'N/A'}）")
        cards = cards if isinstance(cards, list) else []
    for i, c in enumerate(cards):
        h = c.get("heading", "")
        if not isinstance(h, str) or len(h) > HEADING_MAX:
            errors.append(f"卡片{i} heading 需 ≤{HEADING_MAX} 字（当前 {len(h)}）")
        lines = c.get("lines")
        if not isinstance(lines, list) or len(lines) > LINES_MAX:
            errors.append(f"卡片{i} 行数需 ≤{LINES_MAX}（当前 {len(lines) if isinstance(lines, list) else 'N/A'}）")
            lines = lines if isinstance(lines, list) else []
        for j, ln in enumerate(lines):
            if not isinstance(ln, str) or len(ln) > LINE_MAX:
                errors.append(f"卡片{i} 第{j}行 每行需 ≤{LINE_MAX} 字（当前 {len(ln)}）")
    tags = data.get("tags")
    if not isinstance(tags, list) or not (TAGS_MIN <= len(tags) <= TAGS_MAX):
        errors.append(f"tags 需 {TAGS_MIN}~{TAGS_MAX} 个")
    return errors


def main(argv: list[str]) -> int:
    if len(argv) != 2:
        print("用法：python -m scripts.deepdive.xhs_validate <xhs.json>", file=sys.stderr)
        return 2
    data = json.loads(Path(argv[1]).read_text(encoding="utf-8"))
    errors = validate_xhs(data)
    if errors:
        print("✗ xhs.json 校验未过：", file=sys.stderr)
        for e in errors:
            print("  -", e, file=sys.stderr)
        return 1
    print("✓ xhs.json 校验通过")
    return 0


if __name__ == "__main__":
    raise SystemExit(main(sys.argv))
```

- [ ] **Step 5: 运行确认通过**

Run: `cd /Users/jialu/ai-frontier-daily && python -m pytest tests/deepdive/test_xhs_validate.py -q`
Expected: PASS（8 passed）

- [ ] **Step 6: Commit**

```bash
cd /Users/jialu/ai-frontier-daily
git add scripts/deepdive/__init__.py scripts/deepdive/xhs_validate.py tests/deepdive/
git commit -m "feat(deepdive-xhs): xhs.json 硬约束校验"
```

---

## Task 2: xhs.json → render DATA 转换

**Files:**
- Create: `scripts/deepdive/render_xhs.py`（本 task 先做转换纯函数 `build_render_data`）
- Test: `tests/deepdive/test_render_xhs.py`

把 xhs.json + 文章目录 → render.js 要的 DATA：加 `kind:"deepdive"`，`cover` 相对路径 → 绝对路径（不存在的封面文件视为 null），其余透传。

- [ ] **Step 1: 写 failing test**

`tests/deepdive/test_render_xhs.py`:
```python
import json
from pathlib import Path
from scripts.deepdive.render_xhs import build_render_data

FIX = Path(__file__).parent / "fixtures" / "xhs_sample.json"

def load():
    return json.loads(FIX.read_text(encoding="utf-8"))

def test_sets_kind_deepdive(tmp_path):
    data = build_render_data(load(), tmp_path)
    assert data["kind"] == "deepdive"

def test_cover_resolved_to_abspath_when_exists(tmp_path):
    (tmp_path / "assets").mkdir()
    (tmp_path / "assets" / "cover.png").write_bytes(b"x")
    data = build_render_data(load(), tmp_path)
    assert data["cover"] == str((tmp_path / "assets" / "cover.png").resolve())

def test_cover_null_when_missing_file(tmp_path):
    data = build_render_data(load(), tmp_path)   # 没建 cover 文件
    assert data["cover"] is None

def test_passthrough_fields(tmp_path):
    src = load()
    data = build_render_data(src, tmp_path)
    assert data["title"] == src["title"]
    assert data["cards"] == src["cards"]
    assert data["caption"] == src["caption"]
    assert data["tags"] == src["tags"]
```

- [ ] **Step 2: 运行确认失败**

Run: `cd /Users/jialu/ai-frontier-daily && python -m pytest tests/deepdive/test_render_xhs.py -q`
Expected: FAIL（`ImportError: cannot import name 'build_render_data'`）

- [ ] **Step 3: 实现转换纯函数**

`scripts/deepdive/render_xhs.py`:
```python
#!/usr/bin/env python3
"""把专题深度卡片脚本 xhs.json 渲染成小红书图文卡（1080×1440 PNG）。

链路：xhs.json → 校验(xhs_validate) → build_render_data → node render.js → output/deepdive/<slug>/*.png

用法：
  python -m scripts.deepdive.render_xhs content/deepdive/<slug>
  （目录内需有 xhs.json；封面图相对路径写在 xhs.json 的 cover 字段）
"""
import json
import subprocess
import sys
from pathlib import Path

from scripts.deepdive.xhs_validate import validate_xhs

REPO = Path(__file__).resolve().parents[2]
RENDER_JS = REPO / "skills" / "ai-daily-digest" / "render.js"


def build_render_data(xhs: dict, article_dir: Path) -> dict:
    """xhs.json dict + 文章目录 → render.js 要的 DATA。"""
    cover = xhs.get("cover")
    cover_abs = None
    if cover:
        p = (Path(article_dir) / cover).resolve()
        if p.exists():
            cover_abs = str(p)
    return {
        "kind": "deepdive",
        "slug": xhs.get("slug", ""),
        "title": xhs.get("title", ""),
        "cover": cover_abs,
        "cards": xhs.get("cards", []),
        "caption": xhs.get("caption", ""),
        "tags": xhs.get("tags", []),
    }


def main(argv: list[str]) -> int:
    if len(argv) != 2:
        print("用法：python -m scripts.deepdive.render_xhs <article_dir>", file=sys.stderr)
        return 2
    article_dir = Path(argv[1]).resolve()
    xhs_path = article_dir / "xhs.json"
    xhs = json.loads(xhs_path.read_text(encoding="utf-8"))

    errors = validate_xhs(xhs)
    if errors:
        print("✗ xhs.json 校验未过，先修：", file=sys.stderr)
        for e in errors:
            print("  -", e, file=sys.stderr)
        return 1

    data = build_render_data(xhs, article_dir)
    render_json = article_dir / "_render.json"
    render_json.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")

    out_dir = REPO / "output" / "deepdive" / data["slug"]
    out_dir.mkdir(parents=True, exist_ok=True)
    print(f"渲染 → {out_dir}")
    subprocess.run(["node", str(RENDER_JS), str(render_json), str(out_dir)], check=True)
    return 0


if __name__ == "__main__":
    raise SystemExit(main(sys.argv))
```

- [ ] **Step 4: 运行确认通过**

Run: `cd /Users/jialu/ai-frontier-daily && python -m pytest tests/deepdive/test_render_xhs.py -q`
Expected: PASS（4 passed）

- [ ] **Step 5: Commit**

```bash
cd /Users/jialu/ai-frontier-daily
git add scripts/deepdive/render_xhs.py tests/deepdive/test_render_xhs.py
git commit -m "feat(deepdive-xhs): xhs.json→render DATA 转换 + 渲染编排"
```

---

## Task 3: deepdiveCards 卡片构建器（cards.js）

**Files:**
- Modify: `skills/ai-daily-digest/cards.js`（在 `buildDeck` 加 `kind` 分发 + 新增 `deepdiveCards`）
- Test: `skills/ai-daily-digest/test_deepdive_cards.js`

`deepdiveCards(DATA)` → card 对象数组：第 1 张封面卡（有 cover 图用图，无则大标题文字封面），其后每个 cards[i] 一张知识卡。

- [ ] **Step 1: 写 failing test（node 断言脚本）**

`skills/ai-daily-digest/test_deepdive_cards.js`:
```js
const assert = require("assert");
const Cards = require("./cards.js");

const DATA = {
  kind: "deepdive",
  slug: "demo",
  title: "标题示例",
  cover: null,
  cards: [
    { heading: "要点一", lines: ["第一行", "第二行"] },
    { heading: "要点二", lines: ["只有一行"] },
  ],
};

const deck = Cards.buildDeck(DATA);
// 封面 + 2 张知识卡 = 3
assert.strictEqual(deck.length, 3, "卡片数应为封面+2");
// 全部 1080×1440
deck.forEach((c) => {
  assert.strictEqual(c.w, 1080, "宽 1080");
  assert.strictEqual(c.h, 1440, "高 1440");
  assert.ok(/deepdive/.test(c.cls), "cls 含 deepdive");
  assert.ok(c.name, "有 name");
  assert.ok(/inner/.test(c.inner), "inner 含 .inner 容器");
});
// 封面卡含标题文字（无封面图时）
assert.ok(deck[0].inner.includes("标题示例"), "封面卡含标题");
// 知识卡含 heading 与某行
assert.ok(deck[1].inner.includes("要点一"), "知识卡含 heading");
assert.ok(deck[1].inner.includes("第二行"), "知识卡含行文本");
console.log("PASS deepdive cards");
```

- [ ] **Step 2: 运行确认失败**

Run: `cd /Users/jialu/ai-frontier-daily/skills/ai-daily-digest && node test_deepdive_cards.js`
Expected: FAIL（`buildDeck` 现遇到 `kind:"deepdive"` 不返回深度卡，断言 length 失败 / 或抛错）

- [ ] **Step 3: 实现 —— buildDeck 顶部加分发 + 新增 deepdiveCards**

在 `skills/ai-daily-digest/cards.js` 的 `function buildDeck(DATA){` 函数体**第一行**插入分发：
```js
  function buildDeck(DATA){
    if (DATA && DATA.kind === "deepdive") return deepdiveCards(DATA);
```

在 `buildDeck` 之前（同一 IIFE 作用域内）新增构建器（复用文件已有的 `esc`）：
```js
  // 专题深度·小红书知识卡：封面 + 每要点一张。版式见 cards.css 的 .card.deepdive。
  function deepdiveCards(DATA){
    var W = 1080, H = 1440, cards = [];
    // 封面卡
    var coverInner;
    if (DATA.cover) {
      coverInner = '<div class="inner cover-img">'
        + '<img class="cover" src="file://' + esc(DATA.cover) + '">'
        + '<div class="cover-title">' + esc(DATA.title) + '</div></div>';
    } else {
      coverInner = '<div class="inner cover-text"><div class="cover-title">'
        + esc(DATA.title) + '</div></div>';
    }
    cards.push({ name: "00-cover", cls: "deepdive cover", w: W, h: H, inner: coverInner });
    // 知识卡
    (DATA.cards || []).forEach(function(c, i){
      var lines = (c.lines || []).map(function(ln){
        return '<li>' + esc(ln) + '</li>';
      }).join("");
      var inner = '<div class="inner card-body">'
        + '<div class="kicker">' + String(i + 1).padStart(2, "0") + '</div>'
        + '<h2 class="heading">' + esc(c.heading) + '</h2>'
        + '<ul class="lines">' + lines + '</ul></div>';
      var nn = String(i + 1).padStart(2, "0");
      cards.push({ name: nn + "-card", cls: "deepdive", w: W, h: H, inner: inner });
    });
    return cards;
  }
```

- [ ] **Step 4: 运行确认通过**

Run: `cd /Users/jialu/ai-frontier-daily/skills/ai-daily-digest && node test_deepdive_cards.js`
Expected: 打印 `PASS deepdive cards`

- [ ] **Step 5: 回归——早报卡片不受影响**

Run: `cd /Users/jialu/ai-frontier-daily && python -m pytest tests/ -q 2>&1 | tail -1`
Expected: 原有用例全过（深度分发是纯追加，不动早报路径）

- [ ] **Step 6: Commit**

```bash
cd /Users/jialu/ai-frontier-daily
git add skills/ai-daily-digest/cards.js skills/ai-daily-digest/test_deepdive_cards.js
git commit -m "feat(deepdive-xhs): cards.js 加深度知识卡构建器 + buildDeck 分发"
```

---

## Task 4: .card.deepdive 版式（cards.css）+ 端到端渲染核验

**Files:**
- Modify: `skills/ai-daily-digest/cards.css`（追加 `.card.deepdive` 段）
- 验证用：临时 `_render.json` 跑 `render.js`，`sips` 核验尺寸

干净知识卡：白底、克制留白、移动端可读；`overflow:hidden` 作为文字溢出兜底。

- [ ] **Step 1: 追加样式**

在 `skills/ai-daily-digest/cards.css` **文件末尾**追加：
```css
/* ===== 专题深度·小红书知识卡（纯追加，不影响早报版式） ===== */
.card.deepdive{background:#fff;color:#16181d;}
.card.deepdive .inner{padding:96px 84px;justify-content:flex-start;}
.card.deepdive.cover .inner{justify-content:center;align-items:flex-start;}
.card.deepdive .cover-title{font-size:84px;line-height:1.25;font-weight:800;letter-spacing:.5px;}
.card.deepdive .cover-img{position:relative;padding:0;}
.card.deepdive .cover-img img.cover{width:100%;height:100%;object-fit:cover;position:absolute;inset:0;}
.card.deepdive .cover-img .cover-title{position:absolute;left:84px;bottom:96px;right:84px;color:#fff;
  text-shadow:0 2px 24px rgba(0,0,0,.5);font-size:72px;z-index:2;}
.card.deepdive .kicker{font-size:40px;font-weight:700;color:#c0392b;margin-bottom:28px;}
.card.deepdive .heading{font-size:64px;line-height:1.3;font-weight:800;margin:0 0 56px;}
.card.deepdive .lines{list-style:none;padding:0;margin:0;}
.card.deepdive .lines li{font-size:46px;line-height:1.55;margin-bottom:28px;
  padding-left:40px;position:relative;}
.card.deepdive .lines li::before{content:"·";position:absolute;left:8px;color:#c0392b;font-weight:800;}
```

- [ ] **Step 2: 造渲染核验数据（无封面，纯文字卡，避免依赖图片）**

Run（写一个临时 render DATA）:
```bash
cd /Users/jialu/ai-frontier-daily
mkdir -p /tmp/dd_out
cat > /tmp/dd_render.json <<'JSON'
{"kind":"deepdive","slug":"demo","title":"专题深度封面标题示例",
 "cover":null,
 "cards":[
   {"heading":"要点一：标题","lines":["第一行说明文字","第二行说明文字"]},
   {"heading":"要点二：标题","lines":["只有一行"]},
   {"heading":"要点三","lines":["A","B","C"]},
   {"heading":"要点四","lines":["收束一句"]}],
 "caption":"文案","tags":["AI"]}
JSON
```

- [ ] **Step 3: 跑 render.js 渲染**

Run: `cd /Users/jialu/ai-frontier-daily/skills/ai-daily-digest && node render.js /tmp/dd_render.json /tmp/dd_out`
Expected: 打印若干 `✓ 00-cover.*`、`01-card.*`…；`/tmp/dd_out/` 出现 5 张图（封面+4）。
（若报 playwright 未装：`cd skills/ai-daily-digest && npm i playwright && npx playwright install chromium` 后重试；wkhtml 后端亦可。）

- [ ] **Step 4: 核验尺寸 1080×1440**

Run:
```bash
EXT=$(ls /tmp/dd_out | grep -E '00-cover' | sed 's/.*\.//')
sips -g pixelWidth -g pixelHeight /tmp/dd_out/00-cover.$EXT | grep -E "pixelWidth|pixelHeight"
ls /tmp/dd_out | grep -cE 'card|cover'
```
Expected: `pixelWidth: 1080` / `pixelHeight: 1440`；卡片数 5。

- [ ] **Step 5: 清理 + Commit**

```bash
cd /Users/jialu/ai-frontier-daily
rm -rf /tmp/dd_out /tmp/dd_render.json
git add skills/ai-daily-digest/cards.css
git commit -m "feat(deepdive-xhs): .card.deepdive 知识卡版式 + 1080x1440 渲染核验"
```

---

## Task 5: deepdive-xiaohongshu skill（AI 卡片脚本指令）

**Files:**
- Create: `skills/deepdive-xiaohongshu/SKILL.md`

skill 指令：读成稿 → 产 `xhs.json`（遵守硬约束）→ 跑校验/渲染 → 指向发布页。

- [ ] **Step 1: 写 SKILL.md**

`skills/deepdive-xiaohongshu/SKILL.md`:
```markdown
---
name: deepdive-xiaohongshu
description: 把专题深度成稿转换为小红书图文卡（封面+知识卡 1080×1440）+ 笔记文案，复用本仓渲染/发布栈。触发词「发小红书 / 转小红书图文 / 深度内容小红书」。
---

# deepdive-xiaohongshu

把 `content/deepdive/<slug>/draft.md` 成稿稳定转换为小红书图文笔记。

## 步骤

### 1. 生成卡片脚本 xhs.json
读成稿 `content/deepdive/<slug>/draft.md`（及 `assets/` 下封面图），**改写**（不照搬）成 `content/deepdive/<slug>/xhs.json`：

- `slug`：文章目录名去日期前缀。
- `cover`：文章已有封面图相对路径（如 `assets/cover-*.png`）；没有则 `null`。
- `title`：小红书标题，**≤20 字**，点出核心钩子。
- `cards`：**4~8 张**，每张 `{heading, lines}`：
  - `heading` ≤14 字，一个要点。
  - `lines` ≤4 行，每行 ≤22 字，口语化、短句、可独立读。
- `caption`：笔记正文，**≤1000 字**，开头一句抓人 + 分点讲清 + 结尾引导，自然口吻。
- `tags`：1~10 个，如 `["AI","Claude","Agent"]`。

**改写原则**：长文 → 短卡。每卡只承载一个判断/要点，删掉论证细节，留结论与钩子。封面承载总隐喻，正文卡逐点展开。

### 2. 校验 + 渲染
```bash
python -m scripts.deepdive.xhs_validate content/deepdive/<slug>/xhs.json   # 不过线先改
python -m scripts.deepdive.render_xhs content/deepdive/<slug>             # 出图到 output/deepdive/<slug>/
```
校验不过会列出超限项（标题/文案/卡数/行数/每行字数）——按提示缩短重生成，**不要硬发**。

### 3. 发布
- 移动端：打开发布页 `publish.html`「深度内容」入口 → 选当前 slug → 「📲 发送到快捷指令」把图存相册 → 小红书 App 手动发；文案从「🔗 复制文案」取。
- 自动发（可选）：复用 `skills/ai-daily-digest/publish_xhs_newspic.py` 的 xiaohongshu-mcp 链路。

## 稳定性
- 生成期硬约束 + `xhs_validate.py` 拦截（标题≤20/文案≤1000/卡 4~8/heading≤14/行≤4/每行≤22）。
- 版式 `overflow:hidden` 兜底；卡片尺寸固定 1080×1440。
- 封面图自动生成不在本 skill（交 Codex 小黑出图）；本 skill 只用已有封面或文字封面卡。

## 与其他线的关系
- 输入来自专题深度成稿（`firsthand-deepdive` 产出 + article-harness 打磨）。
- 渲染复用 `skills/ai-daily-digest/render.js`（深度卡为 `cards.js`/`cards.css` 纯追加分支）。
- 发布复用早报小红书链路（publish.html / xiaohongshu-mcp）。
```

- [ ] **Step 2: 冒烟——skill 引用的脚本/路径都存在**

Run:
```bash
cd /Users/jialu/ai-frontier-daily
test -f scripts/deepdive/xhs_validate.py && test -f scripts/deepdive/render_xhs.py && echo "脚本 ✓"
test -f publish.html && echo "发布页 ✓"
```
Expected: `脚本 ✓` / `发布页 ✓`

- [ ] **Step 3: Commit**

```bash
cd /Users/jialu/ai-frontier-daily
git add skills/deepdive-xiaohongshu/SKILL.md
git commit -m "feat(deepdive-xhs): deepdive-xiaohongshu skill(卡片脚本指令)"
```

---

## Task 6: publish.html 加「深度内容」入口

**Files:**
- Modify: `publish.html`
- 依赖产物：`output/deepdive/<slug>/manifest.json`（render.js 已自动写，含 `files`）

发布页加一个「深度内容」区：读 `output/deepdive/<slug>/manifest.json` 列图 + 提供「发送到快捷指令 / 复制文案 / 打包 zip」（复用页面已有的同款按钮逻辑）。

- [ ] **Step 1: 读现有 publish.html 结构**

Run: `cd /Users/jialu/ai-frontier-daily && grep -nE "shortcuts://|index.json|function |Tab|tab|fetch\(" publish.html | head -20`
Expected: 看清现有 Tab/数据加载/快捷指令按钮的函数名与结构（复用，不重写）。

- [ ] **Step 2: 加深度内容区（最小接入）**

在 `publish.html` 现有 Tab 容器后追加一个「深度内容」入口区：用 `fetch('output/deepdive/<slug>/manifest.json')` 拉图列表（`manifest.files`），渲染缩略图 + 复用页面已有的「📲 发送到快捷指令 / 🔗 复制链接 / ⬇️ 打包 zip」按钮函数；slug 来自一个输入框或 `output/deepdive/` 列表（若页面是静态托管、无目录列举，则用输入框填 slug，存 `localStorage.deepdive_slug`）。

> 实现细节按 Step 1 看到的现有函数对接：图片 URL 形如 `output/deepdive/<slug>/<file>`，文案从 `content/deepdive/<slug>/xhs.json` 的 `caption`（或渲染时一并写出的 `output/deepdive/<slug>/caption.txt`，见 Step 3）。

- [ ] **Step 3: render_xhs.py 顺手写出 caption.txt 供发布页取用**

在 `scripts/deepdive/render_xhs.py` 的 `main` 里、`subprocess.run(...)` **之前**追加：
```python
    caption = xhs.get("caption", "")
    tags = " ".join("#" + t for t in xhs.get("tags", []))
    (out_dir / "caption.txt").write_text(
        (caption + "\n\n" + tags).strip(), encoding="utf-8")
```

- [ ] **Step 4: 冒烟验证发布页能加载深度产物结构**

Run:
```bash
cd /Users/jialu/ai-frontier-daily
grep -q "deepdive" publish.html && echo "publish.html 已含深度入口 ✓"
```
Expected: `publish.html 已含深度入口 ✓`

- [ ] **Step 5: Commit**

```bash
cd /Users/jialu/ai-frontier-daily
git add publish.html scripts/deepdive/render_xhs.py
git commit -m "feat(deepdive-xhs): publish.html 深度内容入口 + caption.txt 产出"
```

---

## Task 7: 端到端冒烟（Claude Tag 真实成稿）+ 文档

**Files:**
- Modify: `CLAUDE.md`（记录第三条线小红书发布）

- [ ] **Step 1: 对真实成稿生成 xhs.json 并渲染**

用 `deepdive-xiaohongshu` skill 对 `content/deepdive/2026-06-24_introducing-claude-tag/` 生成 `xhs.json`（cover 用该目录已有的小黑封面 `assets/cover-xiaohei-agent-identity.png`），然后：
```bash
cd /Users/jialu/ai-frontier-daily
python -m scripts.deepdive.xhs_validate content/deepdive/2026-06-24_introducing-claude-tag/xhs.json
python -m scripts.deepdive.render_xhs content/deepdive/2026-06-24_introducing-claude-tag
ls output/deepdive/introducing-claude-tag/
```
Expected: 校验通过；`output/deepdive/introducing-claude-tag/` 出现封面（含小黑图）+ N 张知识卡 PNG + `manifest.json` + `caption.txt`。封面图含真实小黑封面（验证 `cover` file:// 路径渲染成功）。

- [ ] **Step 2: 核验封面卡确实嵌入了小黑图（非空白）**

Run:
```bash
cd /Users/jialu/ai-frontier-daily
EXT=$(ls output/deepdive/introducing-claude-tag | grep 00-cover | sed 's/.*\.//')
sips -g pixelWidth -g pixelHeight output/deepdive/introducing-claude-tag/00-cover.$EXT | grep pixel
```
Expected: 1080×1440；人工瞄一眼封面图非纯白（小黑图已嵌入）。

- [ ] **Step 3: CLAUDE.md 记录**

在 `CLAUDE.md` 第三条内容线段落追加：
```markdown
- 小红书发布：`deepdive-xiaohongshu` skill 把成稿 → `xhs.json`（卡片脚本，硬约束校验）→ `scripts/deepdive/render_xhs.py`
  复用 `render.js` 渲染 1080×1440 知识卡到 `output/deepdive/<slug>/` → `publish.html`「深度内容」入口移动端发布
  （快捷指令）或 `publish_xhs_newspic.py`（xiaohongshu-mcp）。设计/计划见 `docs/superpowers/{specs,plans}/2026-06-24-专题深度小红书发布*`。
```

- [ ] **Step 4: 全套测试 + Commit**

```bash
cd /Users/jialu/ai-frontier-daily
python -m pytest tests/ -q 2>&1 | tail -1
git add CLAUDE.md content/deepdive/2026-06-24_introducing-claude-tag/xhs.json output/deepdive/
git commit -m "feat(deepdive-xhs): Claude Tag 端到端冒烟 + CLAUDE.md 记录"
```
Expected: 测试全过。

---

## 完成标准

- [ ] `python -m scripts.deepdive.xhs_validate` 能拦住超限卡片脚本（8 单测过）
- [ ] `python -m scripts.deepdive.render_xhs <article_dir>` 一条命令出 1080×1440 PNG + manifest + caption.txt
- [ ] 深度知识卡是 `cards.js`/`cards.css` 纯追加，早报现有测试/版式不受影响
- [ ] `deepdive-xiaohongshu` skill 文档完整、引用路径可达
- [ ] publish.html 有「深度内容」入口、复用快捷指令链路
- [ ] Claude Tag 真实成稿端到端跑通：封面（含小黑图）+ 知识卡渲染成功
```
