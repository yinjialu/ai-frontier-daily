# AI 前哨 · 手机推送渠道配置（模板）
# ────────────────────────────────────────────────────────────────
# 安装位置（本机私密，勿提交仓库）：
#     ~/.config/ai-frontier-daily/notify.env
# 复制本模板并填值：
#     mkdir -p ~/.config/ai-frontier-daily
#     cp scripts/config-templates/notify.env.example ~/.config/ai-frontier-daily/notify.env
# 配置下面任一渠道即可启用手机推送；多个可叠加。改完即时生效（每次运行重新读取）。
# 一个都不配时，会退回 macOS 桌面通知（不会推到手机）。
# ────────────────────────────────────────────────────────────────

# —— iOS Bark（推荐：App Store 搜 "Bark"，打开 App 复制其中的 device key）——
# BARK_KEY=你的_device_key
# 自托管 Bark 服务器可改根地址（默认 https://api.day.app）：
# BARK_BASE=https://api.day.app

# —— Server 酱（推到你的微信：https://sct.ftqq.com 注册后拿 SendKey）——
# SERVERCHAN_SENDKEY=你的_SendKey

# —— 通用 webhook（会 POST application/json: {"title":..,"body":..}）——
# NOTIFY_WEBHOOK=https://example.com/your-webhook
